interface:
  display_name: "Book2Skill"
  short_description: "Build an indexed skill from uploaded documents"
  default_prompt: "Use $Book2Skill to convert uploaded documents, extract grounded knowledge points, and package a retrieval skill."
